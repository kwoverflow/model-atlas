# Seed Data

Sprint 1 seed data is generated by `python -m app.seed.demo` from the backend container or backend working directory.

All records inserted by the seed workflow are synthetic demonstration data. No external model weights, benchmark datasets, API keys, or production measurements are included.
