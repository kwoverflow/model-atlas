# Structured Request Lab Engineering QA

Date: 2026-09-09. These are automated engineering checks, not human attestation or model quality evidence.

## Browser observation

Request ID: `8d277216-70d6-4a25-9a67-df9417e315a4`.
Query: `AUTOMATED QA - browser contract`.
The natural-language input explicitly identifies this as automated QA, not human approval.

1. Empty form starts unresolved. Selecting set without choosing a value leaves save disabled.
2. Explicit low selection permits draft save. A separate unchecked acknowledgment precedes confirmation.
3. Draft remains available after backend/worker recreation and browser reload.
4. Confirmed query/priority and `local_unverified_assertion` are shown independently from model output.
5. Manually submitted high proposal is blocked with a priority mismatch; simulation button is disabled.
6. The actual local model button generates a new low proposal. The proposal matches, but remains unexecuted
   until the separate simulation button is clicked.
7. Simulation produces `SIM-E2039AA7`, priority low, simulated true. Further execution is disabled.
8. Browser error/warning log observation returned an empty list.

Desktop viewport: 1440 x 1000. Mobile viewport: 390 x 844.
DOM client width equals document scroll width at both sizes. The history table has internal horizontal
scrolling on mobile. Full-page screenshots were inspected for text overlap and nonblank content.

- [Desktop screenshot](desktop.png)
- [Mobile screenshot](mobile.png)

## API observation

[Machine-readable report](../../../artifacts/structured-requests/2026-09-09-api-smoke.json)
was generated by `tools/smoke_structured_requests.py`, not hand-authored.

- Concurrent request: `2a55260e-ac78-44b0-b7db-e65f22f94b22`.
- Concurrent distinct proposal execution returned HTTP 200 and 409. One stored execution, one handler
  invocation, and one simulated-execution event were observed. Repeating the winner returned its receipt.
- Wrong priority was blocked before execution.
- Edited request: `58f6c07d-8571-49f0-85f8-9722cbe9c78c`. Saving revision 2 clears confirmation and
  invalidates the old proposal execution.
- A separate actual model proposal matched its automated structured QA input. It was not executed by
  the API smoke script. Recorded generation has two model HTTP request bodies, 591 prompt tokens and
  58 completion tokens; usage fields may include adapter estimates and are not a cost/performance study.
- Official overview fields were identical before and after the smoke run: BLOCKED, 110 critical
  failures, 384 actual results, output review 0/30, not_production_ready.

## Automated suites

- Feature suite: 49 passed.
- Host full suite: 535 passed, 1 skipped.
- Docker full suite: 536 passed.
- Host and Docker Ruff, frontend lint, TypeScript and Docker frontend build: passed.
- Existing 47-table metadata hash remains pinned; two additive lab tables use migration 202609090001.
- Prior diagnostic chain independently re-audited through `tools/review_ticket_priority.py`.
- Finalized 1.0.4 source validation: 64/64 approved, critical 20/20; corpus hash unchanged.

## Limits

No new human review was submitted. No production authorization, external ticket execution or
model-quality improvement is established. Simulated transactional concurrency is not an external
exactly-once guarantee. Live expiry was not tested by waiting 15 minutes in the browser; expiry,
revocation and in-flight invalidation are covered by focused backend tests.
